export default defineAppConfig({
    themes: {
        // Each themes must have matching styles in theme.css
        // ⚠️ Do not remove these defaults for proper fallback behavior
        // Themes should be defined using the format: "Label": "class"
        Auto: 'auto',
        Light: 'light',
        Dark: 'dark',
    },
})
